version: 2

sphinx:
  version: latest
  configuration: docs/source/conf.py

python:
  version: 3.8
  install:
    - requirements: docs/requirements.txt

build:
  os: ubuntu-22.04
  tools:
    - sphinx
    - pip

formats:
  - htmlzip
