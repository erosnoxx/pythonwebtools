version: 2

# Build settings
sphinx:
  version: latest  # ou '3.x' se quiser usar uma versão específica
  configuration: docs/conf.py

python:
  version: 3.8  # ou outra versão do Python que você deseja usar
  install:
    - requirements: docs/requirements.txt  # Se você tiver dependências adicionais

# Options for the documentation
formats:
  - htmlzip

# Optional: custom settings
# You can add environment variables if needed
